import styles from './Header.module.css';

const Header: React.FC = () => {
  return (
    <>
      <header className={styles['header']}>Her we will work with forms</header>
    </>
  );
};

export default Header;
